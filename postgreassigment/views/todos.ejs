<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Todo List</title>
</head>
<body>
<h2>Todos</h2>

<ul>
    <% todos.forEach(todo => { %>
        <li><strong><%= todo.title %></strong> - <%= todo.date.toDateString() %></li>
    <% }); %>
</ul>

<form action="/add-todo" method="post">
    <input type="text" name="title" placeholder="Enter new todo" required>
    <button type="submit">Add Todo</button>
</form>
</body>
</html>
